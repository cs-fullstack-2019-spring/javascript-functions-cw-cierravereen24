//Funct
// Using the code below:
// ```javascript
// printNumbers();
// ```
// Create a ```printNumbers``` function to print integers from -25 to 20.


// The function prints numbers -25 to 20 through a loop.

function printNumbers(i) {

    for (i = -25; i <= 20; i++)
    {
        console.log(i);
    }
}
printNumbers();

